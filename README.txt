I made this to help clean out my gmail account. The python script
will change the labels of emails to remove them from the inbox. 
It uses a Gmail API setup in Google Cloud Platform (GCP) with my 
engineering account. The OAuth credentials file is stored locally
and was genreated via GCP OAuth. 